/**
 * Marker: monster has an active weapon reservoir DoT (the shared burn-family pool
 * that drains as poison/fire/frost damage — see systems/weaponFamilies.ts BURN_FAMILY).
 * One marker covers every reservoir-DoT weapon; the void-corruption reservoir uses
 */
export interface HasWeaponDot {}
