import { GATE_THICK } from '@mmo-idle/shared';

export { GATE_THICK };
export const GATE_COLOR = 0x00ffdd;

// Minimap layout constants. Square, matching the square node — the old 220x165
// was exactly the node's former 4:3. Width came down rather than height going up
// so the map stays roughly its old footprint next to the Auto Combat control.
export const MM_W = 165;
export const MM_H = 165;
export const MM_PAD = 8;
