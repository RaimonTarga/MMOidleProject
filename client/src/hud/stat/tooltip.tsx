// The hover-tooltip hook moved to `hud/primitives/HelpTooltip` once skill nodes,
// ability effect lines and the class-mechanic frames started explaining
// themselves the same way the character sheet does. Re-exported here so the
// stat panel's imports stay put.
export { useHoverTooltip } from '../primitives/HelpTooltip';
