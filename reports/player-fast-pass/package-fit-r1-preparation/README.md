# Package-fit r1 preparation evidence

Prepared, not executed. Main allocation: 36, executed: 0. Two separately declared
combat smokes qualified new Slam/Brace recording; neither is a main observation.

Use `docs/briefs/player-package-fit-operator-packet.md` for the Luna handoff.
`build-changes.json` is the complete original/revised package table, including exact
applied gear/upgrades and ordered destinations. `report-contract.json` requires
machine-readable results and a scoped commit/push for repository access by another agent.

`identity.json`, `manifest.json`, `resolved-builds.json`, `ready.json` and `smoke/`
are original qualification records copied without relabelling from the local
`package-fit-r1-preparation` directory. Their HEAD is the base checkout with declared
uncommitted experiment adapters; their file hashes bind the actual qualified bytes.
The separate local `package-fit-r1-execution-packet` binds those identical bytes to
the commit introducing the new specification, preserving originals in this directory.
Execution must verify that bound identity; publication and measurement revisions differ.

Original opening evidence remains in `../opening-run-01-review/`. No original losses
are invalidated by selecting more suitable packages. Historical Boss1/Boss2 references
are context, not replay controls. Full suite and browser playtest were not run.
