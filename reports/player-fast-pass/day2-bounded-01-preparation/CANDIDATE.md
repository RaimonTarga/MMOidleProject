# Block A disposition — qualified experimental correctness candidate

One correction is isolated on `codex/day2-session-candidate`; it is **not adopted into develop or Block B**. The complete difference from the pinned control is CANDIDATE.patch, including the focused test. No damage, shield amount/cadence, summon health, reconstruction, HP-cost or item treatment is included.

The production World runs class mechanics/death collection before monster AI and combat. `collectDeaths` invokes `despawnMinion`, whose `dropMonsterAggroOnMinion` detaches aggro. AI and combat also clear a selected dead physical minion. A subsequent actual formation attack reacquires aggro; `setAggroTarget` then assigns a new `sinceMs`, and `refreshEnemyShieldState` treats it as a new session and refills the barrier. The focused control World tick records this exact sequence in one tick: despawn clear at 10,100 ms, new aggro at 10,100, shield-session reset, then a real hit consuming the refreshed shield. The test starts at session 10,000 with a partially consumed barrier. This establishes ordering beyond the historical sampled diagnosis.

Existing normal handover already retains `sinceMs`. The candidate uses that path before clearing a dead summon target, only when the ordinary targeting policy selects a live, actively targeting member of the **same owner/formation**. It requires a live same-node owner, same-node dead selected body, no move command or return, and the existing pursuit leash. It does not add pull range, preserve idle nearby bodies, or create a grace timer. It cancels the lost-victim cast/engage sequence and clears the old attack target while keeping the engagement timestamp. Layout changes, bulk dismissal, departure, death of the owner, no continuing attacker and leash failure keep the old reset behavior. The current selector must actually choose an eligible participant; this is deliberately conservative, not a guarantee of continuity in every formation arrangement.

The helper is called from AI validation, combat validation and individual death despawn. Bulk owner despawn never invokes handover. This preserves all session-scoped mechanics consistently through a continuing engagement rather than special-casing shield amounts. Genuine new sessions still rearm opening strikes, timers and shields. No source indicates that killing one physical summon is intended to reset an otherwise continuing formation encounter; the ordinary handover rule supports treating this as a candidate correctness repair.

Focused tests cover all three production paths and a full World tick, continued absorption, normal handover, owner continuation, ordinary player targeting, explicit disengagement/re-entry, owner departure, leash failure, all bodies dead, idle bodies, live and bulk dismissal, and pending-cast cancellation. Control reproduces refresh; candidate retains the old timestamp and consumed shield. Summoner overhaul/specializations and monster primitive/empowered/mobile-cast checks pass. These tests are **not balance observations** and do not establish five-minute efficacy, throughput or live feel.

Both A arms use identical opt-in read-only hooks. Synchronous session events include monster identity/type, previous shield-session counter, current aggro session/target, HP, barrier and awareness. Formation-target changes are sampled after each tick; ordinary event streams retain attacks, absorption, kills and deaths. Count actual target transitions from IDs, not merely the number of setter calls. Shield-session hooks fire before reinitialization; subsequent damage and samples show its consequence. Idle/no-aggro shield fallback events are distinct from engaged renewals. A row without relevant engaged Glacier Bear shield exposure is unexposed for that mechanism, still usable for general regression.

Historical Defensive Tundra evidence remains the previously published 4,313 HP bear, 2,151 zero-HP/14-absorbed hits and 94 sampled aggro sessions. No historical measurement is relabeled as this candidate's outcome. Luna's eight allocated A observations determine efficacy; command center decides adoption.
