# V2 measurement contract

## Inputs, clocks and restoration

Every measured actor descends from a fresh character on the candidate revision with ordinary acquisition and reward/time multipliers 1. Earn prerequisite tier-entry and specified +3-stage checkpoints separately; record their full elapsed acquisition cost and lineage. Independent P tiers consume already available exact entries, never outputs of another measured P tier. Two exposures are independent world instances, not asserted deterministic seeds. No historical-wallet conversion, grants, deleted costs or outcome-based choice of the richest entry.

An R matched set restores one immutable snapshot with exact progression/inventory/ordered skills/Rune ownership, abilities, stances, Rites, wallets, partial catalysts, class specialization and summoner logical slots. Hash original input and persistent readbacks. Runtime normalization must be declared and identical; no ready-minion grants. No purchases or build changes during measured windows. Starting states are fixed; ordinary earned progression is retained. Do not use `fixedBiomeMasteryPlayers`, which suppresses XP rewards. At the start of every arm verify full persistent equality. Travel can alter it: until a qualified adapter handles that boundary, these are not runnable rate cells. Record setup/approach and attrition separately, never silently reset earned wallets or teleport into purported live-progression evidence.

Use simulated elapsed time; timeScale 1 keeps current taint rules intact. Dead, recovery, idle and return-travel time inside the window stay in the denominator. Natural repopulation and respawns continue. Missing telemetry is null; an aborted/short window is not a completed 20-minute sample. Exclude setup from rate time but retain setup costs. General P tier time includes travel, death recovery and acquisition; tutorial is separate from T1 entry.

## Rates and reconciliation

From the production reward ledger measure kills, deaths, HP damage received (separate shielding), essence credited by colour, XP credited by biome, catalysts by family including fractions, plus dead/recovery/travel/idle time and actual population samples. Authored density/modifier/spawn factors are configuration, not observed density. Distinguish incidental boss/add contributions; explicit-zero or raised adds must contribute no unintended income.

For simulated hours H, family income is `(ending units - starting units + units spent + (ending progress - starting progress)/100)/H`. In R, spend must equal zero. Reconcile every colour/family as `initial + all credits - all debits = final`, with fractional catalyst progress included. Preserve refunds/other sources explicitly. Rates never come from tooltips or projected tables.

XP caps are a competing event. T1's five-minute target is shorter than a 20-minute window, so total-window XP/hour alone would systematically favour slower uncapped arms. Report both whole-window credited XP and rate over the actual uncapped subwindow, capped timestamp, remaining XP and exposure. Never reset mastery or extrapolate capped zero as low productivity. Threshold overshoot stays visible. Hours-to-cap from rates is labelled a projection; observed mastery time belongs to P. At-cap XP comparisons are not applicable, missing values unavailable, and zero/zero uninformative.

Same-biome comparisons can include every tested modifier; additionally retain same-modifier audit pairs as mechanistic comparisons. Colour/family best-node comparisons must use actual relevant colour/family and exact actor/stage/input identity. Public recipe demand determines relevance; whole catalogue coverage is an over-coverage check, not a claim every system is mandatory. Label best-tested results and show numerator/denominator node IDs, incomes, deaths and downtime. No pooled reward score.

## Ratios and confirmations

Ratios are **current/lower**. Targets are 1.25 for essence/uncapped XP and 1.10 for same-family catalysts. <1 is inversion; 1–<1.10 is near parity; any positive ratio below its metric's target is a target miss. Missing/invalid metrics are unavailable. Positive/zero is unbounded; zero/zero is uninformative. Do not accept a point-estimate pass as certainty.

After all R1/R1M rows have a terminal disposition, predeclared confirmation selects every below-target matched pair (including all inversions), deduplicated across metrics, plus up to eight passing pairs with smallest ratio/target margin (tie by pair ID). Use **two independent 60-minute replicas of both nodes from the same original input**, same source/build, no adaptive changes. Target pairs from both stages remain distinct. Confirmation cells are generated/sealed after selection; none exist as measured results now. Preserve all screen results and uncertainty. If confirmation volume is operationally excessive, report pending pairs and revise the next packet openly, never silently drop failures.

## Mastery, funding and adoption

For every biome segment, record target threshold from `biomeXpForBiomeLevel` and starting remainder. The 5/15/30/60-minute targets refer to a full current-tier six-level segment. Report partial inherited segments separately, and both local active farming and gross elapsed biome-leg time. Do not grade a partial segment against a full-segment duration without explicitly labelled normalization. Also report retired-biome caps.

For each predeclared primary item and essential system:

1. **Mastery clock:** biome entry, every level/unlock, segment cap, GM and local upgrade gate crossings.
2. **Funding clock:** first ordinary wallet state sufficient for the exact missing legal acquisition path and upgrades, checking each colour/family separately. Keep resource-only sufficiency distinct from current gate eligibility. Record earlier actual debits toward that item and competing purchases. An optional gross-income earmarking calculation is a counterfactual with no double allocation, not spendable live wallet evidence.
3. **Adoption clock:** craft/top-up/evolve/reconstruct, actual +1..+5 debits and equip/attunement. Core/relic have no +N track.

Report funding and adoption separately for +3 and +5. GM +3 gates are 18/55/97/139; GM +5 gates 30/72/114/156. Local gates are item-specific. The primary +5 target of 25–50% extra farming is a single-item resource-effort hypothesis, not a full-loadout or wall-clock tier requirement. Report both item-attributable gross funding versus segment income and actual additional farming with all competing purchases. Do not claim a raw price/income ratio is measured extra time.

Premium continuation: one primary item per T2+ actor/replica must be chosen by the class route before sealing. Begin after the core boundary and required gates are available, retaining actual wallet/gear; if it was already funded, report that fact rather than force extra grind. Proposed extra cap is twice the tier's mastery target (30/60/120 simulated minutes), fixed across actors in that tier and frozen before execution. If required mastery is absent, report gate-limited/not-started rather than farming missing tier mastery under the name of a currency premium. Up to 24 continuations; exact item/route/input bindings remain a launch gate.

## Bottlenecks and summaries

Record every next-declared-purchase/gate predicate change and failed attempt with wallet/mastery/RP state. Exclusive time classes: productive/unblocked, combat failure/recovery, travel/route, boss/seal, biome/recipe gate, GM/upgrade gate, catalyst, essence, other/unknown. Use that precedence when multiple constraints coexist; retain simultaneous blockers separately. Percentages sum over actual elapsed time; co-occurrence does not establish causality. Distinguish income too low, wrong currency farm, unaffordable planned answer, and legally equipped answer losing combat.

Milestones: tier entry; every biome entry/leg complete/mastery level; recipe unlock; craft/evolution/reconstruction; each upgrade; ability/Rune/stance/Rite acquisition and attunement/RP reservation; catalyst mint; boss readiness/attempt/clear/seal; tier advance and authored terminal. Preserve first tier advance and full-route completion separately. T4 terminal remains to be designated from an approved playtest route, never invented as T5 advance.

Show per tier/actor/replica durations, censoring, item/system timings, spend and leftovers by currency, predecessor top-up versus current-item spend, safety and bottlenecks. Two P replicas give descriptive spread, not reliable population quantiles. Show both observations and completed-case median/min/max with censored counts; do not turn a 24-hour cap into a completion value. No averaging unlike classes/tiers into one success verdict. No dynamic results are present in this packet.
