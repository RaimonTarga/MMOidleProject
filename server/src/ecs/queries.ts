/**
 * Cross-cutting ECS queries live here.
 *
 * Per-feature queries (cadence players, monsters with knockback, etc.) are
 * colocated with the system that owns them. This file is a thin barrel
 * reserved for queries shared between multiple subsystems.
 */
export {};
